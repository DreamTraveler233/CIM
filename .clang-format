BasedOnStyle: Google                # 以 Google C++ 风格为基础
IndentWidth: 4                      # 缩进宽度为4个空格
TabWidth: 4                         # Tab字符宽度为4（但实际不使用Tab）
UseTab: Never                       # 禁止使用Tab，统一用空格缩进
ColumnLimit: 100                    # 每行最大字符数120，超出则自动换行
SortIncludes: true                  # 自动排序头文件include语句
DerivePointerAlignment: false       # 不自动推导指针对齐方式，使用PointerAlignment指定
PointerAlignment: Left              # 指针*靠左，如 int* ptr;
AllowShortFunctionsOnASingleLine: Inline # 允许短函数单行显示（如inline函数）
Standard: Cpp11                     # 采用C++11标准
SpaceBeforeParens: ControlStatements # 控制语句前加空格，如 if (...)，函数声明/调用不加
BreakBeforeBraces: Attach           # 大括号不独占一行（K&R/Attach风格）
ReflowComments: false               # 不自动重排注释文本，保持原样